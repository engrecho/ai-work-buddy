# AI-Buddy 私有运维资料（已挪走）

> **📂 本目录已挪到 Workspace 根目录共享目录：`../../JustForMe/AI-buddy.md`**
>
> AI 助手和项目所有者请去 Workspace 根目录的 `JustForMe/` 查阅。
> 本目录只保留 `.gitkeep` 占位 + `README.legacy.md` 历史快照。

## 新约定

- **共享运维资料**：`/Users/jaylon/Documents/SyncFiles/AI_Workspace/JustForMe/AI-buddy.md`
- **共享目录总入口**：`/Users/jaylon/Documents/SyncFiles/AI_Workspace/JustForMe/README.md`
- **本目录状态**：仍被 `.gitignore` 屏蔽，仅保留占位文件，不会被提交

## 为什么挪走

- 一个工程一份 `JustForMe/` → 信息分散，难维护
- 统一到 Workspace 根目录 → 所有项目共享一份
- 添加新工程的运维信息时，**只在 Workspace/JustForMe/ 下新增 `<project>.md` 即可**
