node_modules
build
.DS_Store
.env
.env.local
.env.*.local
server/node_modules

# 个人 / 测试产物，不入库
.workbuddy/
*.mp4
*.mov
*.avi
__test__/
server/__test__/

# 私有运维资料统一在 Workspace 根目录的 README.md
# 各项目内不再保留 JustForMe/（已迁移到 Workspace 根 README）
JustForMe/
CLAUDE.md

# 部署执行记录（本地生成，不入库）
deploy/once/.done
deploy/.applied_migrations